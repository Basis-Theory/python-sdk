# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .payment_credential_metadata_format import PaymentCredentialMetadataFormat
from .payment_credential_metadata_provider import PaymentCredentialMetadataProvider
from .shared_payment_amount import SharedPaymentAmount
from .shared_payment_merchant import SharedPaymentMerchant


class PaymentCredentialMetadata(UniversalBaseModel):
    """
    Credential metadata. Never contains card numbers, SPT values, or MPP payloads.
    """

    id: str
    allowance_id: str
    payment_method_id: str
    rail: str
    provider: PaymentCredentialMetadataProvider
    format: PaymentCredentialMetadataFormat
    provider_ids: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Safe provider-native references for support. Never contains a spendable credential.
    """

    amount: SharedPaymentAmount
    merchant: typing.Optional[SharedPaymentMerchant] = pydantic.Field(default=None)
    """
    Merchant the spend was authorized against, taken from the allowance or from the credential request. Absent on credentials minted before merchant was recorded.
    """

    status: typing.Literal["created"] = pydantic.Field(default="created")
    """
    Credential lifecycle status. Only `created` exists today — the service has no settlement visibility, so a credential is never observed being spent.
    """

    expires_at: dt.datetime
    created_at: dt.datetime

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
