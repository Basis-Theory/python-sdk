# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .connection_authorization_details_item import ConnectionAuthorizationDetailsItem
from .connection_next_action import ConnectionNextAction
from .connection_permissions import ConnectionPermissions
from .connection_principal import ConnectionPrincipal
from .connection_status import ConnectionStatus
from .connection_status_details import ConnectionStatusDetails


class Connection(UniversalBaseModel):
    id: str
    provider: str
    grant_method: str = pydantic.Field()
    """
    How the authorization was established. A property of the grant, not of the provider — one provider may offer several.
    """

    principal: ConnectionPrincipal
    status: ConnectionStatus
    status_details: ConnectionStatusDetails
    permissions: ConnectionPermissions
    authorization_details: typing.Optional[typing.List[ConnectionAuthorizationDetailsItem]] = pydantic.Field(
        default=None
    )
    """
    RFC 9396 authorization details actually granted by the provider.
    """

    next_action: typing.Optional[ConnectionNextAction] = None
    authorization_expires_at: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    When the authority itself lapses, if the provider tells us. Absent means no scheduled end, which is the common case — most providers on this roster never expire an authorization. This is deliberately not the bearer credential's own expiry, which is internal.
    """

    created_at: dt.datetime
    updated_at: dt.datetime

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
