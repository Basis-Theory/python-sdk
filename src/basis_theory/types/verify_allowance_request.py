# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .verify_allowance_request_complete_provider import VerifyAllowanceRequestCompleteProvider
from .verify_allowance_request_complete_rail import VerifyAllowanceRequestCompleteRail
from .verify_allowance_request_start_device_context import VerifyAllowanceRequestStartDeviceContext
from .verify_allowance_request_start_provider import VerifyAllowanceRequestStartProvider
from .verify_allowance_request_start_rail import VerifyAllowanceRequestStartRail
from .verify_allowance_request_submit_session_session_context import VerifyAllowanceRequestSubmitSessionSessionContext


class StartVerifyAllowanceRequest(UniversalBaseModel):
    """
    Each action is only valid for the providers that run that ceremony: Visa (`vic`) uses the session/OTP/passkey actions, Mastercard (`agentpay`) uses `complete`, and an interactive Stripe Link allowance (`virtual-card`/`link`) uses `start` to obtain the consumer's approval redirect and `complete` to check it once. A rail with no cardholder ceremony (`spt`, or a delegated `virtual-card` allowance) accepts `start` and answers `400 VERIFICATION_NOT_REQUIRED`.
    """

    action: typing.Literal["start"] = "start"
    rail: VerifyAllowanceRequestStartRail
    provider: VerifyAllowanceRequestStartProvider
    display_name: typing.Optional[str] = None
    device_context: typing.Optional[VerifyAllowanceRequestStartDeviceContext] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class SubmitSessionVerifyAllowanceRequest(UniversalBaseModel):
    """
    Each action is only valid for the providers that run that ceremony: Visa (`vic`) uses the session/OTP/passkey actions, Mastercard (`agentpay`) uses `complete`, and an interactive Stripe Link allowance (`virtual-card`/`link`) uses `start` to obtain the consumer's approval redirect and `complete` to check it once. A rail with no cardholder ceremony (`spt`, or a delegated `virtual-card` allowance) accepts `start` and answers `400 VERIFICATION_NOT_REQUIRED`.
    """

    action: typing.Literal["submit_session"] = "submit_session"
    rail: typing.Literal["agentic-token"] = "agentic-token"
    provider: typing.Literal["vic"] = "vic"
    session_context: VerifyAllowanceRequestSubmitSessionSessionContext

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CompleteVerifyAllowanceRequest(UniversalBaseModel):
    """
    Each action is only valid for the providers that run that ceremony: Visa (`vic`) uses the session/OTP/passkey actions, Mastercard (`agentpay`) uses `complete`, and an interactive Stripe Link allowance (`virtual-card`/`link`) uses `start` to obtain the consumer's approval redirect and `complete` to check it once. A rail with no cardholder ceremony (`spt`, or a delegated `virtual-card` allowance) accepts `start` and answers `400 VERIFICATION_NOT_REQUIRED`.
    """

    action: typing.Literal["complete"] = "complete"
    rail: VerifyAllowanceRequestCompleteRail
    provider: VerifyAllowanceRequestCompleteProvider

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class SelectOtpMethodVerifyAllowanceRequest(UniversalBaseModel):
    """
    Each action is only valid for the providers that run that ceremony: Visa (`vic`) uses the session/OTP/passkey actions, Mastercard (`agentpay`) uses `complete`, and an interactive Stripe Link allowance (`virtual-card`/`link`) uses `start` to obtain the consumer's approval redirect and `complete` to check it once. A rail with no cardholder ceremony (`spt`, or a delegated `virtual-card` allowance) accepts `start` and answers `400 VERIFICATION_NOT_REQUIRED`.
    """

    action: typing.Literal["select_otp_method"] = "select_otp_method"
    rail: typing.Literal["agentic-token"] = "agentic-token"
    provider: typing.Literal["vic"] = "vic"
    method_id: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class SubmitOtpVerifyAllowanceRequest(UniversalBaseModel):
    """
    Each action is only valid for the providers that run that ceremony: Visa (`vic`) uses the session/OTP/passkey actions, Mastercard (`agentpay`) uses `complete`, and an interactive Stripe Link allowance (`virtual-card`/`link`) uses `start` to obtain the consumer's approval redirect and `complete` to check it once. A rail with no cardholder ceremony (`spt`, or a delegated `virtual-card` allowance) accepts `start` and answers `400 VERIFICATION_NOT_REQUIRED`.
    """

    action: typing.Literal["submit_otp"] = "submit_otp"
    rail: typing.Literal["agentic-token"] = "agentic-token"
    provider: typing.Literal["vic"] = "vic"
    otp_code: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class SubmitPasskeyVerifyAllowanceRequest(UniversalBaseModel):
    """
    Each action is only valid for the providers that run that ceremony: Visa (`vic`) uses the session/OTP/passkey actions, Mastercard (`agentpay`) uses `complete`, and an interactive Stripe Link allowance (`virtual-card`/`link`) uses `start` to obtain the consumer's approval redirect and `complete` to check it once. A rail with no cardholder ceremony (`spt`, or a delegated `virtual-card` allowance) accepts `start` and answers `400 VERIFICATION_NOT_REQUIRED`.
    """

    action: typing.Literal["submit_passkey"] = "submit_passkey"
    rail: typing.Literal["agentic-token"] = "agentic-token"
    provider: typing.Literal["vic"] = "vic"
    assurance_data: typing.Dict[str, typing.Any]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


VerifyAllowanceRequest = typing_extensions.Annotated[
    typing.Union[
        StartVerifyAllowanceRequest,
        SubmitSessionVerifyAllowanceRequest,
        CompleteVerifyAllowanceRequest,
        SelectOtpMethodVerifyAllowanceRequest,
        SubmitOtpVerifyAllowanceRequest,
        SubmitPasskeyVerifyAllowanceRequest,
    ],
    pydantic.Field(discriminator="action"),
]
