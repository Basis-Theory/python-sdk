# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import datetime as dt
import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.pagination import AsyncPager, SyncPager
from ...core.request_options import RequestOptions
from ...types.allowance import Allowance
from ...types.allowance_list import AllowanceList
from ...types.allowance_verification_response import AllowanceVerificationResponse
from ...types.shared_payment_amount import SharedPaymentAmount
from ...types.shared_payment_merchant import SharedPaymentMerchant
from ...types.shared_payment_provider_error import SharedPaymentProviderError
from ...types.shared_payment_provider_error_list import SharedPaymentProviderErrorList
from ...types.verify_allowance_request import VerifyAllowanceRequest
from .raw_client import AsyncRawAllowancesClient, RawAllowancesClient
from .types.allowances_list_request_status import AllowancesListRequestStatus

if typing.TYPE_CHECKING:
    from .credentials.client import AsyncCredentialsClient, CredentialsClient
    from .rails.client import AsyncRailsClient, RailsClient
# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AllowancesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAllowancesClient(client_wrapper=client_wrapper)
        self._client_wrapper = client_wrapper
        self._rails: typing.Optional[RailsClient] = None
        self._credentials: typing.Optional[CredentialsClient] = None

    @property
    def with_raw_response(self) -> RawAllowancesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAllowancesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        size: typing.Optional[int] = None,
        start: typing.Optional[str] = None,
        payment_method_id: typing.Optional[str] = None,
        status: typing.Optional[AllowancesListRequestStatus] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Allowance, AllowanceList]:
        """
        Lists allowances for the current tenant. Defaults to active, unexpired resources; use `status=all` for a complete Portal history. Results can be scoped to one payment method, and server-side page filling prevents sparse pages when filtering by status.

        Parameters
        ----------
        size : typing.Optional[int]

        start : typing.Optional[str]

        payment_method_id : typing.Optional[str]
            Optional payment method ID to list its allowances.

        status : typing.Optional[AllowancesListRequestStatus]
            Derived resource status filter. Defaults to active.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Allowance, AllowanceList]
            Paginated allowances

        Examples
        --------
        from basis_theory import BasisTheory

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        response = client.agentic.allowances.list(
            size=1,
            start="start",
            payment_method_id="payment_method_id",
            status="active",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            size=size, start=start, payment_method_id=payment_method_id, status=status, request_options=request_options
        )

    def create(
        self,
        *,
        payment_method_id: str,
        amount: SharedPaymentAmount,
        description: str,
        expires_at: dt.datetime,
        bt_idempotency_key: typing.Optional[str] = None,
        agent_id: typing.Optional[str] = OMIT,
        merchant: typing.Optional[SharedPaymentMerchant] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Allowance:
        """
        Create a spending allowance from a payment method. Supply `merchant` to scope the mandate to one merchant, or omit it to leave the allowance open and name a merchant on each credential request instead. The payment method must have at least one enabled rail; otherwise the request returns `NO_ACTIVE_RAILS`.

        Parameters
        ----------
        payment_method_id : str

        amount : SharedPaymentAmount

        description : str
            Customer-facing prompt describing what the allowance permits.

        expires_at : dt.datetime
            ISO 8601 timestamp when the allowance expires. Must be in the future.

        bt_idempotency_key : typing.Optional[str]
            Optional stable key for safely replaying this create request. Without it, every request creates a new allowance.

        agent_id : typing.Optional[str]
            Optional attribution to an agent owned by the tenant. This does not authorize the caller; tenant API-key permissions remain the authorization boundary.

        merchant : typing.Optional[SharedPaymentMerchant]
            Optional merchant the allowance is scoped to. Omit it to leave the allowance open and supply `merchant` on each credential request instead. Once set it cannot be changed, and a credential request for a merchant-scoped allowance must not send its own `merchant`.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Public integration metadata. The JSON-encoded value must not exceed 32 KiB.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Allowance
            Allowance created

        Examples
        --------
        import datetime

        from basis_theory import BasisTheory, SharedPaymentAmount

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        client.agentic.allowances.create(
            payment_method_id="payment_method_id",
            amount=SharedPaymentAmount(
                value="100.00",
                currency="USD",
            ),
            description="description",
            expires_at=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.create(
            payment_method_id=payment_method_id,
            amount=amount,
            description=description,
            expires_at=expires_at,
            bt_idempotency_key=bt_idempotency_key,
            agent_id=agent_id,
            merchant=merchant,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def get(self, allowance_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Allowance:
        """
        Parameters
        ----------
        allowance_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Allowance
            Allowance

        Examples
        --------
        from basis_theory import BasisTheory

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        client.agentic.allowances.get(
            allowance_id="allowance_id",
        )
        """
        _response = self._raw_client.get(allowance_id, request_options=request_options)
        return _response.data

    def delete(self, allowance_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Cancel an allowance so new credentials cannot be created from it. Network-side purchase instructions held by its rails are cancelled with the provider.

        Parameters
        ----------
        allowance_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from basis_theory import BasisTheory

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        client.agentic.allowances.delete(
            allowance_id="allowance_id",
        )
        """
        _response = self._raw_client.delete(allowance_id, request_options=request_options)
        return _response.data

    def update(
        self,
        allowance_id: str,
        *,
        amount: typing.Optional[SharedPaymentAmount] = OMIT,
        description: typing.Optional[str] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Allowance:
        """
        Updates one or more mutable fields by changing the provider-side mandate first, then committing the same amount, prompt, and expiry locally. Mints are blocked while the update is in flight, and an empty request body is rejected.

        Parameters
        ----------
        allowance_id : str

        amount : typing.Optional[SharedPaymentAmount]

        description : typing.Optional[str]
            Customer-facing prompt describing what the allowance permits.

        expires_at : typing.Optional[dt.datetime]
            ISO 8601 timestamp when the allowance expires. Must be in the future.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Allowance
            Updated allowance

        Examples
        --------
        from basis_theory import BasisTheory

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        client.agentic.allowances.update(
            allowance_id="allowance_id",
        )
        """
        _response = self._raw_client.update(
            allowance_id, amount=amount, description=description, expires_at=expires_at, request_options=request_options
        )
        return _response.data

    def errors(
        self,
        allowance_id: str,
        *,
        size: typing.Optional[int] = None,
        start: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[SharedPaymentProviderError, SharedPaymentProviderErrorList]:
        """
        Lists sanitized provider failures for an allowance, including failed verification and credential attempts. Raw provider bodies and card data are never returned.

        Parameters
        ----------
        allowance_id : str

        size : typing.Optional[int]

        start : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[SharedPaymentProviderError, SharedPaymentProviderErrorList]
            Paginated sanitized provider errors

        Examples
        --------
        from basis_theory import BasisTheory

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        response = client.agentic.allowances.errors(
            allowance_id="allowance_id",
            size=1,
            start="start",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.errors(allowance_id, size=size, start=start, request_options=request_options)

    def verify(
        self,
        allowance_id: str,
        *,
        request: VerifyAllowanceRequest,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AllowanceVerificationResponse:
        """
        Start or continue self-served verification for a rail that requires it. Public and private applications may call this operation with `agentic:allowance:verify`; browser clients should use a public application key. Visa verification is advanced through explicit ceremony actions. Mastercard managed authentication is finalized with `complete` after the hosted ceremony; callback delivery is only a browser signal and is not required.

        Parameters
        ----------
        allowance_id : str

        request : VerifyAllowanceRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AllowanceVerificationResponse
            Verification envelope: `{ status, rail, next_action? }`. `next_action.type` is one of `passkey_session` (initialize Visa's hosted iframe, then send its secure token with `submit_session`), `select_otp_method`, `otp`, `passkey`, or `redirect` (Mastercard managed authentication, or the consumer's approval of an interactive Stripe Link allowance). Repeat calls until `status` is `active`. BT validates every terminal transition server-to-server with the network — a client can never self-assert an active rail. Visa non-code step-up methods are not supported and return `VERIFICATION_NOT_SUPPORTED` when no supported OTP method is available.

        Examples
        --------
        from basis_theory import BasisTheory, StartVerifyAllowanceRequest

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        client.agentic.allowances.verify(
            allowance_id="allowance_id",
            request=StartVerifyAllowanceRequest(
                rail="agentic-token",
                provider="vic",
            ),
        )
        """
        _response = self._raw_client.verify(allowance_id, request=request, request_options=request_options)
        return _response.data

    @property
    def rails(self):
        if self._rails is None:
            from .rails.client import RailsClient  # noqa: E402

            self._rails = RailsClient(client_wrapper=self._client_wrapper)
        return self._rails

    @property
    def credentials(self):
        if self._credentials is None:
            from .credentials.client import CredentialsClient  # noqa: E402

            self._credentials = CredentialsClient(client_wrapper=self._client_wrapper)
        return self._credentials


class AsyncAllowancesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAllowancesClient(client_wrapper=client_wrapper)
        self._client_wrapper = client_wrapper
        self._rails: typing.Optional[AsyncRailsClient] = None
        self._credentials: typing.Optional[AsyncCredentialsClient] = None

    @property
    def with_raw_response(self) -> AsyncRawAllowancesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAllowancesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        size: typing.Optional[int] = None,
        start: typing.Optional[str] = None,
        payment_method_id: typing.Optional[str] = None,
        status: typing.Optional[AllowancesListRequestStatus] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Allowance, AllowanceList]:
        """
        Lists allowances for the current tenant. Defaults to active, unexpired resources; use `status=all` for a complete Portal history. Results can be scoped to one payment method, and server-side page filling prevents sparse pages when filtering by status.

        Parameters
        ----------
        size : typing.Optional[int]

        start : typing.Optional[str]

        payment_method_id : typing.Optional[str]
            Optional payment method ID to list its allowances.

        status : typing.Optional[AllowancesListRequestStatus]
            Derived resource status filter. Defaults to active.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Allowance, AllowanceList]
            Paginated allowances

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.agentic.allowances.list(
                size=1,
                start="start",
                payment_method_id="payment_method_id",
                status="active",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            size=size, start=start, payment_method_id=payment_method_id, status=status, request_options=request_options
        )

    async def create(
        self,
        *,
        payment_method_id: str,
        amount: SharedPaymentAmount,
        description: str,
        expires_at: dt.datetime,
        bt_idempotency_key: typing.Optional[str] = None,
        agent_id: typing.Optional[str] = OMIT,
        merchant: typing.Optional[SharedPaymentMerchant] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Allowance:
        """
        Create a spending allowance from a payment method. Supply `merchant` to scope the mandate to one merchant, or omit it to leave the allowance open and name a merchant on each credential request instead. The payment method must have at least one enabled rail; otherwise the request returns `NO_ACTIVE_RAILS`.

        Parameters
        ----------
        payment_method_id : str

        amount : SharedPaymentAmount

        description : str
            Customer-facing prompt describing what the allowance permits.

        expires_at : dt.datetime
            ISO 8601 timestamp when the allowance expires. Must be in the future.

        bt_idempotency_key : typing.Optional[str]
            Optional stable key for safely replaying this create request. Without it, every request creates a new allowance.

        agent_id : typing.Optional[str]
            Optional attribution to an agent owned by the tenant. This does not authorize the caller; tenant API-key permissions remain the authorization boundary.

        merchant : typing.Optional[SharedPaymentMerchant]
            Optional merchant the allowance is scoped to. Omit it to leave the allowance open and supply `merchant` on each credential request instead. Once set it cannot be changed, and a credential request for a merchant-scoped allowance must not send its own `merchant`.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Public integration metadata. The JSON-encoded value must not exceed 32 KiB.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Allowance
            Allowance created

        Examples
        --------
        import asyncio
        import datetime

        from basis_theory import AsyncBasisTheory, SharedPaymentAmount

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agentic.allowances.create(
                payment_method_id="payment_method_id",
                amount=SharedPaymentAmount(
                    value="100.00",
                    currency="USD",
                ),
                description="description",
                expires_at=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            payment_method_id=payment_method_id,
            amount=amount,
            description=description,
            expires_at=expires_at,
            bt_idempotency_key=bt_idempotency_key,
            agent_id=agent_id,
            merchant=merchant,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def get(self, allowance_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Allowance:
        """
        Parameters
        ----------
        allowance_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Allowance
            Allowance

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agentic.allowances.get(
                allowance_id="allowance_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(allowance_id, request_options=request_options)
        return _response.data

    async def delete(self, allowance_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Cancel an allowance so new credentials cannot be created from it. Network-side purchase instructions held by its rails are cancelled with the provider.

        Parameters
        ----------
        allowance_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agentic.allowances.delete(
                allowance_id="allowance_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(allowance_id, request_options=request_options)
        return _response.data

    async def update(
        self,
        allowance_id: str,
        *,
        amount: typing.Optional[SharedPaymentAmount] = OMIT,
        description: typing.Optional[str] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Allowance:
        """
        Updates one or more mutable fields by changing the provider-side mandate first, then committing the same amount, prompt, and expiry locally. Mints are blocked while the update is in flight, and an empty request body is rejected.

        Parameters
        ----------
        allowance_id : str

        amount : typing.Optional[SharedPaymentAmount]

        description : typing.Optional[str]
            Customer-facing prompt describing what the allowance permits.

        expires_at : typing.Optional[dt.datetime]
            ISO 8601 timestamp when the allowance expires. Must be in the future.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Allowance
            Updated allowance

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agentic.allowances.update(
                allowance_id="allowance_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            allowance_id, amount=amount, description=description, expires_at=expires_at, request_options=request_options
        )
        return _response.data

    async def errors(
        self,
        allowance_id: str,
        *,
        size: typing.Optional[int] = None,
        start: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[SharedPaymentProviderError, SharedPaymentProviderErrorList]:
        """
        Lists sanitized provider failures for an allowance, including failed verification and credential attempts. Raw provider bodies and card data are never returned.

        Parameters
        ----------
        allowance_id : str

        size : typing.Optional[int]

        start : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[SharedPaymentProviderError, SharedPaymentProviderErrorList]
            Paginated sanitized provider errors

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.agentic.allowances.errors(
                allowance_id="allowance_id",
                size=1,
                start="start",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.errors(allowance_id, size=size, start=start, request_options=request_options)

    async def verify(
        self,
        allowance_id: str,
        *,
        request: VerifyAllowanceRequest,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AllowanceVerificationResponse:
        """
        Start or continue self-served verification for a rail that requires it. Public and private applications may call this operation with `agentic:allowance:verify`; browser clients should use a public application key. Visa verification is advanced through explicit ceremony actions. Mastercard managed authentication is finalized with `complete` after the hosted ceremony; callback delivery is only a browser signal and is not required.

        Parameters
        ----------
        allowance_id : str

        request : VerifyAllowanceRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AllowanceVerificationResponse
            Verification envelope: `{ status, rail, next_action? }`. `next_action.type` is one of `passkey_session` (initialize Visa's hosted iframe, then send its secure token with `submit_session`), `select_otp_method`, `otp`, `passkey`, or `redirect` (Mastercard managed authentication, or the consumer's approval of an interactive Stripe Link allowance). Repeat calls until `status` is `active`. BT validates every terminal transition server-to-server with the network — a client can never self-assert an active rail. Visa non-code step-up methods are not supported and return `VERIFICATION_NOT_SUPPORTED` when no supported OTP method is available.

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory, StartVerifyAllowanceRequest

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agentic.allowances.verify(
                allowance_id="allowance_id",
                request=StartVerifyAllowanceRequest(
                    rail="agentic-token",
                    provider="vic",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.verify(allowance_id, request=request, request_options=request_options)
        return _response.data

    @property
    def rails(self):
        if self._rails is None:
            from .rails.client import AsyncRailsClient  # noqa: E402

            self._rails = AsyncRailsClient(client_wrapper=self._client_wrapper)
        return self._rails

    @property
    def credentials(self):
        if self._credentials is None:
            from .credentials.client import AsyncCredentialsClient  # noqa: E402

            self._credentials = AsyncCredentialsClient(client_wrapper=self._client_wrapper)
        return self._credentials
