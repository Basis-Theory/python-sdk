# This file was auto-generated by Fern from our API Definition.

import typing

from ....core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ....core.pagination import AsyncPager, SyncPager
from ....core.request_options import RequestOptions
from ....types.payment_credential import PaymentCredential
from ....types.payment_credential_list import PaymentCredentialList
from ....types.payment_credential_metadata import PaymentCredentialMetadata
from ....types.shared_payment_amount import SharedPaymentAmount
from ....types.shared_payment_merchant import SharedPaymentMerchant
from .raw_client import AsyncRawCredentialsClient, RawCredentialsClient
from .types.create_payment_credential_request_credential import CreatePaymentCredentialRequestCredential
from .types.create_payment_credential_request_provider import CreatePaymentCredentialRequestProvider
from .types.create_payment_credential_request_rail import CreatePaymentCredentialRequestRail

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CredentialsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCredentialsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCredentialsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCredentialsClient
        """
        return self._raw_client

    def list(
        self,
        allowance_id: str,
        *,
        size: typing.Optional[int] = None,
        start: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PaymentCredentialMetadata, PaymentCredentialList]:
        """
        List credential metadata for an allowance. Responses contain metadata only — never card numbers, SPT values, or MPP payloads.

        Parameters
        ----------
        allowance_id : str

        size : typing.Optional[int]

        start : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PaymentCredentialMetadata, PaymentCredentialList]
            Paginated credential metadata

        Examples
        --------
        from basis_theory import BasisTheory

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        response = client.agentic.allowances.credentials.list(
            allowance_id="allowance_id",
            size=1,
            start="start",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(allowance_id, size=size, start=start, request_options=request_options)

    def create(
        self,
        allowance_id: str,
        *,
        rail: CreatePaymentCredentialRequestRail,
        provider: CreatePaymentCredentialRequestProvider,
        credential: CreatePaymentCredentialRequestCredential,
        bt_idempotency_key: typing.Optional[str] = None,
        amount: typing.Optional[SharedPaymentAmount] = OMIT,
        merchant: typing.Optional[SharedPaymentMerchant] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentCredential:
        """
        Create a spend credential from an allowance. Supply BT-IDEMPOTENCY-KEY for retry protection. Without it, every request is a new mint and may spend the allowance again.

        Parameters
        ----------
        allowance_id : str

        rail : CreatePaymentCredentialRequestRail

        provider : CreatePaymentCredentialRequestProvider

        credential : CreatePaymentCredentialRequestCredential

        bt_idempotency_key : typing.Optional[str]
            Optional stable key for detecting retries. A successful bearer credential cannot be replayed.

        amount : typing.Optional[SharedPaymentAmount]

        merchant : typing.Optional[SharedPaymentMerchant]
            Merchant this credential is being minted for. Required when the allowance has no `merchant`, and rejected when it does — the allowance's merchant is the scope the cardholder verified against, so a mint can neither restate nor replace it. Required on every rail for consistency. At mint Visa (`vic`) forwards it to the network, and Stripe Link (`link`) names it on the spend request the consumer sees.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentCredential
            Payment credential

        Examples
        --------
        from basis_theory import BasisTheory
        from basis_theory.agentic.allowances.credentials import (
            CardCreatePaymentCredentialRequestCredential,
        )

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        client.agentic.allowances.credentials.create(
            allowance_id="allowance_id",
            rail="agentic-token",
            provider="vic",
            credential=CardCreatePaymentCredentialRequestCredential(),
        )
        """
        _response = self._raw_client.create(
            allowance_id,
            rail=rail,
            provider=provider,
            credential=credential,
            bt_idempotency_key=bt_idempotency_key,
            amount=amount,
            merchant=merchant,
            request_options=request_options,
        )
        return _response.data

    def get(
        self, allowance_id: str, credential_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PaymentCredentialMetadata:
        """
        Get credential metadata. The credential payload itself (card number, SPT, MPP token) is only ever returned by the create call.

        Parameters
        ----------
        allowance_id : str

        credential_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentCredentialMetadata
            Credential metadata

        Examples
        --------
        from basis_theory import BasisTheory

        client = BasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )
        client.agentic.allowances.credentials.get(
            allowance_id="allowance_id",
            credential_id="credential_id",
        )
        """
        _response = self._raw_client.get(allowance_id, credential_id, request_options=request_options)
        return _response.data


class AsyncCredentialsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCredentialsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCredentialsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCredentialsClient
        """
        return self._raw_client

    async def list(
        self,
        allowance_id: str,
        *,
        size: typing.Optional[int] = None,
        start: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PaymentCredentialMetadata, PaymentCredentialList]:
        """
        List credential metadata for an allowance. Responses contain metadata only — never card numbers, SPT values, or MPP payloads.

        Parameters
        ----------
        allowance_id : str

        size : typing.Optional[int]

        start : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PaymentCredentialMetadata, PaymentCredentialList]
            Paginated credential metadata

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.agentic.allowances.credentials.list(
                allowance_id="allowance_id",
                size=1,
                start="start",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(allowance_id, size=size, start=start, request_options=request_options)

    async def create(
        self,
        allowance_id: str,
        *,
        rail: CreatePaymentCredentialRequestRail,
        provider: CreatePaymentCredentialRequestProvider,
        credential: CreatePaymentCredentialRequestCredential,
        bt_idempotency_key: typing.Optional[str] = None,
        amount: typing.Optional[SharedPaymentAmount] = OMIT,
        merchant: typing.Optional[SharedPaymentMerchant] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentCredential:
        """
        Create a spend credential from an allowance. Supply BT-IDEMPOTENCY-KEY for retry protection. Without it, every request is a new mint and may spend the allowance again.

        Parameters
        ----------
        allowance_id : str

        rail : CreatePaymentCredentialRequestRail

        provider : CreatePaymentCredentialRequestProvider

        credential : CreatePaymentCredentialRequestCredential

        bt_idempotency_key : typing.Optional[str]
            Optional stable key for detecting retries. A successful bearer credential cannot be replayed.

        amount : typing.Optional[SharedPaymentAmount]

        merchant : typing.Optional[SharedPaymentMerchant]
            Merchant this credential is being minted for. Required when the allowance has no `merchant`, and rejected when it does — the allowance's merchant is the scope the cardholder verified against, so a mint can neither restate nor replace it. Required on every rail for consistency. At mint Visa (`vic`) forwards it to the network, and Stripe Link (`link`) names it on the spend request the consumer sees.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentCredential
            Payment credential

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory
        from basis_theory.agentic.allowances.credentials import (
            CardCreatePaymentCredentialRequestCredential,
        )

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agentic.allowances.credentials.create(
                allowance_id="allowance_id",
                rail="agentic-token",
                provider="vic",
                credential=CardCreatePaymentCredentialRequestCredential(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            allowance_id,
            rail=rail,
            provider=provider,
            credential=credential,
            bt_idempotency_key=bt_idempotency_key,
            amount=amount,
            merchant=merchant,
            request_options=request_options,
        )
        return _response.data

    async def get(
        self, allowance_id: str, credential_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PaymentCredentialMetadata:
        """
        Get credential metadata. The credential payload itself (card number, SPT, MPP token) is only ever returned by the create call.

        Parameters
        ----------
        allowance_id : str

        credential_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentCredentialMetadata
            Credential metadata

        Examples
        --------
        import asyncio

        from basis_theory import AsyncBasisTheory

        client = AsyncBasisTheory(
            correlation_id="YOUR_CORRELATION_ID",
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agentic.allowances.credentials.get(
                allowance_id="allowance_id",
                credential_id="credential_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(allowance_id, credential_id, request_options=request_options)
        return _response.data
